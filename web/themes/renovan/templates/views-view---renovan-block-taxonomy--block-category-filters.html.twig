{% if rows %}
    <div class="button-group filters-button-group">

        {% if header %}
            {{ header }}
        {% endif %}
        {% for row in rows %}
            {{ row }}
        {% endfor %}
    </div>
{% endif %}
