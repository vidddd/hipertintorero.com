  <section class="big-title {{ clase }}">
      <div class="container">
        <div class="row">
         <div class="col-xs-12">
          {% block content %}
          {% if pre != '' %}<h1>{{ pre }} /{% else %} <h1>{% endif %}
           {{ content }}
          {% endblock %}
          </div>
        </div>
      </div>
</section>
